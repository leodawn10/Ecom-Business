# Ambient audio

Drop a seamless loop here named `ambient-temple.mp3` (soft sitar drone +
distant temple bells, ~30–60s, < 1 MB, normalised to ~-18 LUFS).

The `SoundToggle` component plays it on user opt-in and fails silently if the
file is absent — so the experience never breaks when it is missing.
